using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace MethodQuiz
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]

        public string ReturnTheWord(string Fizz, string Buzz, string FizzBuzz, int numOne)
        {
            numbOne = 0;

            while (true)
            {
                if (numOne / 3)
                {
                    Console.WriteLine(string Fizz);
                    break;

                }
                // break
                numOne++;

                numOne = 0;

                while (true)
                {
                    if (numbOne / 5)
                    {
                        Console.WriteLine(string Buzz);
                        break;

                    }
                    // break
                    numOne++;
                    numOne = 0;

                    while (true)
                    {
                        if (numbOne / 3 || numbOne / 5)
                        {
                            Console.WriteLine(string FizzBuzz);
                            break;

                        }
                        // break
                        numOne++;
                    }
                    public int Age(DateTime DateOfBirth)
                    {

                        get
                        {
                            TimeSpan ageSpan = DateTime.Now - DateOfBirth;
                            double totalAgeInYears = ageSpan.TotalDays / 365;
                            int yearsOfAge = Convert.ToInt32(Math.Floor(totalAgeInYears));
                            return yearsOfAge;
                        }


                    }

        public static class StringConversion
        {
            public static void Main()
            {
                string input = String.Empty;

                try

                {

                    int result = Int32.Parse(input);

                    Console.WriteLine(result);

                }

             }
        }
        public void SayHello(string name)
        {
            Console.WriteLine($"Hello, {name}!");
        }
        public void SimpleOperators()
        {
            int a = 22;
            int b = 15;
            int division = a / b;
            Console.WriteLine(double division);


        }
        public void MaxMethod()
        {
            int a = 22;
            int b = 15;

            Console.WriteLine(MaxMethod(int a, int b);


        }
    }






